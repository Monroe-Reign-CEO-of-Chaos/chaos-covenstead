**Textarea** — the multi-line glass field, matching `Input`.

```jsx
<Textarea label="Your Prophecy" rows={5} placeholder="Write it as scripture…" />
```

- `label`, `hint`, `invalid`, `rows`. Passes through to `<textarea>`. Vertical resize only.
